import { FilterRoutePipe } from '../filter-route.pipe';

describe('FilterRoutePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterRoutePipe();
    expect(pipe).toBeTruthy();
  });
});
