import { GetFormStatusPipe } from './get-form-status.pipe';

describe('GetFormStatusPipe', () => {
  it('create an instance', () => {
    const pipe = new GetFormStatusPipe();
    expect(pipe).toBeTruthy();
  });
});
