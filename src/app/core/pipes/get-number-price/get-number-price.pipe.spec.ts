import { GetNumberPricePipe } from './get-number-price.pipe';

describe('GetNumberPricePipe', () => {
  it('create an instance', () => {
    const pipe = new GetNumberPricePipe();
    expect(pipe).toBeTruthy();
  });
});
